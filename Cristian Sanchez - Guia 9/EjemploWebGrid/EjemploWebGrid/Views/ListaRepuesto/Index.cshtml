@model IEnumerable<EjemploWebGrid.Models.repuestos>

@{
    ViewBag.Title = "Lista Repuestos";
    var grid = new WebGrid(source: Model, columnNames: new[]
    { "nombre", "precio", "descuento" }, defaultSort: "precio");
    grid.Pager(WebGridPagerModes.All);
}

<h2>Lista Repuestos</h2>

<div id="gridContent" style="font-family: Arial; padding: 20px;">
@grid.GetHtml(
    tableStyle: "webgrid-table",
    headerStyle: "webgrid-header",
    footerStyle: "webgrid-footer",
    alternatingRowStyle: "webgrid-alternating-row",
    selectedRowStyle: "webgrid-selected-row",
    rowStyle: "webgrid-row-style",
    columns: grid.Columns(
        grid.Column(columnName: "nombre", header: "Nombre Repuesto"),
        grid.Column(columnName: "precio", header: "Precio Venta"),
        grid.Column(columnName: "descuento", header: "% Desc.")
        //grid.Column(columnName: "Editar", format: (item) => {
            //var link = Html.ActionLink("Editar", "Edit", new { id = item.id_rep });
            //return link;
        //})
    )
)
</div>