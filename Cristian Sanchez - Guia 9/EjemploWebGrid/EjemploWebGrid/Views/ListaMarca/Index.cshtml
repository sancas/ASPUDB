@model IEnumerable<EjemploWebGrid.Models.marca>

@{
    ViewBag.Title = "Index";
    var grid = new WebGrid(source: Model, columnNames: new[]
    { "marca1", "pais" }, defaultSort: "marca1");
    grid.Pager(WebGridPagerModes.All);
}

<h2>Lista Marca</h2>

<div id="gridContent" style="font-family: Arial; padding: 20px;">
@grid.GetHtml(
    tableStyle: "webgrid-table",
    headerStyle: "webgrid-header",
    footerStyle: "webgrid-footer",
    alternatingRowStyle: "webgrid-alternating-row",
    selectedRowStyle: "webgrid-selected-row",
    rowStyle: "webgrid-row-style",
    columns: grid.Columns(
        grid.Column(columnName: "marca1", header: "Marca"),
        grid.Column(columnName: "pais", header: "Pais de Ensamblaje")
    )
)
</div>