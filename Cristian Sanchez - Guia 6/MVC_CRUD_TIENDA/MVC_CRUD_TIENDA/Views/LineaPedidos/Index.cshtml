@model IEnumerable<MVC_CRUD_TIENDA.Models.LINEAPEDIDO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PEDIDO.ID_PEDIDO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRODUCTO.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UNIDADES)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PEDIDO.ID_PEDIDO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUCTO.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UNIDADES)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_LINEAPEDIDO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_LINEAPEDIDO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_LINEAPEDIDO })
        </td>
    </tr>
}

</table>
